on:
  push:
    branches:
      - master1








jobs:
  build:
    runs-on: ubuntu-latest
      

    steps: 
      - name: 'test action'
        run: pwd

      - name: 'test action1'
        run: echo 'manar zain'

      - name: 'test action2'
        run: ls